# PostgreSQL local development user.
POSTGRES_USER=app
# PostgreSQL local development password; replace outside local use.
POSTGRES_PASSWORD=app_secret
# PostgreSQL database name.
POSTGRES_DB=app
# Browser API URL.
NEXT_PUBLIC_API_URL=http://localhost:8080
# Frontend server-to-server backend origin.
API_ORIGIN=http://backend:8080
