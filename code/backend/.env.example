# PostgreSQL connection string; required.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable
# HTTP listen port; runtime injects this.
PORT=8080
# Optional fallback when PORT is absent.
APP_PORT=8080
